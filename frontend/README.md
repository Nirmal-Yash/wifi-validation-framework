# NetRegress Runner UI

React + Vite operational shell for the canonical /api/v1 Runner API. It covers authentication, Run launch/list/detail, cancellation, retry, lifecycle/result inspection, health and artifact views. RBAC remains enforced by the API.
