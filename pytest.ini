[pytest]
pythonpath = .
markers =
    smoke: Quick connectivity checks
    regression: Full regression suite
    perf: Performance and throughput tests
    recovery: Fault-injection and service recovery validation
    real_lab: Tests requiring the protected GNS3/mac80211_hwsim environment
testpaths = tests
addopts = -v --tb=short
